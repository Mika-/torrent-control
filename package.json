{
  "name": "torrent-control",
  "version": "0.1.0",
  "repository": "git@github.com:Mika-/torrent-control.git",
  "license": "MIT",
  "scripts": {
    "start": "web-ext run --source-dir ./src/",
    "startdev": "web-ext run --firefox=firefoxdeveloperedition --source-dir ./src/",
    "build": "web-ext build --source-dir ./src/",
    "test": "mocha"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^6.2.0",
    "rewire": "^4.0.1",
    "sinon-chrome": "^3.0.1",
    "web-ext": "^4.0.0"
  }
}
